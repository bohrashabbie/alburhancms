version: "3.9"

services:
  backend:
    build: .
    container_name: alburhan-cms-api
    restart: unless-stopped
    ports:
      - "8002:8001"
    environment:
      - DATABASE_URL=postgresql://postgres:Sha2558161@13.60.4.75:5432/alburhancms
      - SECRET_KEY=alburhan-cms-secret-key-2024-change-in-production
      - ALGORITHM=HS256
      - ACCESS_TOKEN_EXPIRE_MINUTES=1440
      - CORS_ORIGINS=http://localhost,http://localhost:80,http://13.60.4.75
      - UPLOAD_DIR=uploads
    volumes:
      - uploads_data:/app/uploads

volumes:
  uploads_data:
